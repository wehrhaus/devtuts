/**
 * Simple test to ensure testing is configured correctly to use `npm test`
 */
import expect from 'expect';

describe('Mocha Config Test', () => {

    it('should work', () => {
        expect(true).toEqual(true);
    });

});